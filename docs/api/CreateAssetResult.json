{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CreateAssetResult",
  "description": "The return object of a `vsl_createAsset` request",
  "type": "object",
  "required": [
    "asset_id",
    "claim_id"
  ],
  "properties": {
    "asset_id": {
      "description": "The ID (hex-encoded 256 bit hash) of the asset",
      "type": "string"
    },
    "claim_id": {
      "description": "Settled claim ID for the create asset command  (hex-encoded 256 bit hash)",
      "type": "string"
    }
  }
}