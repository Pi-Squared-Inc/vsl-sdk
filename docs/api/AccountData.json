{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AccountData",
  "description": "Collected public data about an account",
  "type": "object",
  "required": [
    "asset_balances",
    "balance",
    "nonce"
  ],
  "properties": {
    "nonce": {
      "description": "the account nonce (64 bit unsigned integer)",
      "type": "integer",
      "format": "uint64",
      "minimum": 0.0
    },
    "balance": {
      "description": "the account native token balance (u128 formatted as hex string).",
      "type": "string"
    },
    "asset_balances": {
      "description": "The balances of all assets held by the account.",
      "type": "object",
      "additionalProperties": {
        "type": "string"
      }
    },
    "state": {
      "description": "the current state of the account (a 256-bit hash), or `None` if unset.",
      "type": [
        "string",
        "null"
      ]
    }
  }
}